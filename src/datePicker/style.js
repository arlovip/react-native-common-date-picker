import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    datePickerContainer: {
        flexDirection: 'row',
        justifyContent: 'center',
    },
    datePickerListItemView: {
        justifyContent: 'center',
        alignItems: 'center',
    },
});
